---
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="footer-inner">
    <div class="footer-left">
      <span class="footer-name">Giorgio Vanini</span>
      <span class="footer-sep">/</span>
      <span>Roma 🇮🇹🇪🇺</span>
    </div>
    <div class="footer-right">
      <a href="https://github.com/sakn0m" target="_blank" rel="noopener noreferrer">GitHub</a>
      <a href="https://www.linkedin.com/in/giorgio-vanini-8627123a4/" target="_blank" rel="noopener noreferrer">LinkedIn</a>
      <a href="https://jojo.news" target="_blank" rel="noopener noreferrer">jojo.news</a>
    </div>
  </div>
  <div class="footer-bottom">
    <span>&copy; {year}</span>
  </div>
</footer>

<style>
  .site-footer {
    background-color: var(--ink);
    border-top: 3px solid var(--eu-blue);
    padding: var(--space-lg) 0 var(--space-md);
    font-size: 0.8rem;
    color: var(--paper);
  }

  .footer-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-md);
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }

  .footer-left {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    opacity: 0.6;
  }

  .footer-name {
    font-family: var(--font-display);
    font-weight: 800;
    text-transform: uppercase;
    font-size: 0.75rem;
    letter-spacing: 0.05em;
  }

  .footer-sep {
    opacity: 0.3;
  }

  .footer-right {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
  }

  .footer-right a {
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.7rem;
    border-bottom: 2px solid transparent;
    padding-bottom: 2px;
    transition: border-color 0.15s;
  }

  .footer-right a:hover {
    border-bottom-color: var(--gold);
  }

  .footer-bottom {
    max-width: 1200px;
    margin: var(--space-md) auto 0;
    padding: var(--space-sm) var(--space-md) 0;
    border-top: 1px solid rgba(245, 240, 232, 0.08);
    opacity: 0.3;
    font-size: 0.7rem;
  }
</style>
